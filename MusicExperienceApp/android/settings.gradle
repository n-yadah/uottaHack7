pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        google()
    }
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

plugins {
    id("com.facebook.react.settings")
}

// Apply the React Native plugin configuration
rootProject.name = "MusicExperienceAppNative"
include(":app")
includeBuild("../node_modules/@react-native/gradle-plugin")
