import { SOLACE_HOST, SOLACE_USERNAME, SOLACE_PASSWORD } from '@env';

export const solaceConfig = {
    host: SOLACE_HOST,
    username: SOLACE_USERNAME,
    password: SOLACE_PASSWORD,
    clientId: "MusicExperienceApp",
  };